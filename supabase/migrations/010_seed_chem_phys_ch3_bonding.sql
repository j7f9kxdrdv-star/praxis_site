-- ============================================================
-- Praxis Prep — Chem/Phys Chapter 3: Bonding & Chemical Interactions
-- ============================================================
-- RETIRED 2026-06-15.
--
-- This migration originally seeded 150 pre-v5 "Bonding & Chemical
-- Interactions" questions. They predate the v5 authoring pipeline and
-- violated current scope/format rules: heavy out-of-AAMC-5B-scope content
-- (lattice energy, Born-Haber, Madelung constants, molecular-orbital /
-- bonding-antibonding theory, coordinate-covalent bonds as a named topic,
-- and quantitative p=qd / Debye dipole-moment calculations), bare flat
-- notation instead of inline LaTeX, explanations lacking the (Choice X)
-- per-distractor format, and no foundation / content_category / discipline /
-- cognitive_skill / estimated_time_seconds classification or
-- question_distractor_metadata rows.
--
-- The 150 rows are REPLACED by the v5 seed in supabase/scripts/
-- genchem_ch3_batch{1,2,3,4}.sql (90 questions, AAMC foundation 5B,
-- topic = 'Bonding & Chemical Interactions', adversarially verified and
-- audit-clean). batch1 of that seed also performs this same topic-scoped
-- purge before inserting, so a live database is reconciled either way.
--
-- Body replaced with an idempotent DELETE so re-running the migration
-- chain on a fresh database purges any legacy rows before the new scripts
-- seed clean ones. Safe to run repeatedly (no-op when no such rows exist).
-- ============================================================

DELETE FROM questions WHERE topic = 'Bonding & Chemical Interactions';
